# Overrides the image's baked-in spark-defaults.conf. Same "demo" REST/Iceberg
# catalog as the tabulario/spark-iceberg image default, plus s3.path-style-access
# — without it, the S3 client addresses buckets as "warehouse.minio:9000"
# (virtual-hosted style), which MinIO can't resolve without MINIO_DOMAIN set,
# and every table write fails with NoSuchBucketException even though the
# bucket exists.
spark.sql.extensions                   org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions
spark.sql.catalog.demo                 org.apache.iceberg.spark.SparkCatalog
spark.sql.catalog.demo.type            rest
spark.sql.catalog.demo.uri             http://rest:8181
spark.sql.catalog.demo.io-impl         org.apache.iceberg.aws.s3.S3FileIO
spark.sql.catalog.demo.warehouse       s3://warehouse/wh/
spark.sql.catalog.demo.s3.endpoint     http://minio:9000
spark.sql.catalog.demo.s3.path-style-access true
spark.sql.defaultCatalog               demo
spark.eventLog.enabled                 true
spark.eventLog.dir                     /home/iceberg/spark-events
spark.history.fs.logDirectory          /home/iceberg/spark-events
spark.sql.catalogImplementation        in-memory
